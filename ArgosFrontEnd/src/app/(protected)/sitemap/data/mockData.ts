import { ICategoryData } from '../types/sitemap.types';

export const mockCategory: ICategoryData = {
    name: "Inspecciones",
    routes: [{ name: "Generar Inspección", path: "/inspections/create" }]
}
